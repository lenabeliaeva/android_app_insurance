<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.insurance">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="28">
        <activity android:name=".AddingExistingPoliceActivity" />
        <activity android:name=".ProlongationActivity" />
        <activity android:name=".PoliceActivity" />
        <activity android:name=".PasswordActivity" />
        <activity
            android:name=".FillingUserActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".CalculationActivity" />
        <activity
            android:name=".UserActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".productList.ProductListActivity" />
        <activity android:name=".FillingPoliceActivity" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignInActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/network_security_config" />

        <activity android:name=".TopLevelScreen.TopLevelActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>